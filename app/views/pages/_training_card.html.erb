
  <%= link_to training_path(training) do %>
  <div class="result-card" >
  <div class="photo-result-card" style="background-image: url('<%= cl_image_path training.profile.trainer.banner_picture %>')">
    </div>
    <div class="description-result-card">
      <ul class="list-inline title-card">
        <li><h3><%=training.title%> |</h3></li>
        <li><h3><%=training.profile.first_name + " " + training.profile.last_name%></h3></li>
        <li><p>Date: <%= training.sessions.first.date %></p></li>
        <li><p><%= training.sessions.first.time.strftime("at %I:%M%p")%></p></li>
        <li><p>Duration: <%= training.duration %> minutes </p></li>
        <li><p>Location: <%=training.address %></p></li>
        <li><p>Price: €<%= training.price %></p></li>
      </ul>
<%= link_to "Book", training_path(training), class: "navbar-wagon-item navbar-wagon-button btn", id:"book-button" %>
    </div>
  </div>
  <% end %>




