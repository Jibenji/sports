<div class="hidden-xs">
  <div class="banner-container">
    <div class="banner-content">
      <div class="banner-title">
        <h1>
         Soplex
        </h1>
        <h3>
         Train Whenever, Wherever
        </h3>
      </div>

     <div class="btn-down fa fa-chevron-down fa-3x" aria-hidden="true" onclick="">
     </div>

     <div class="form-inline search-field-banner text-center">
      <%= simple_form_for :search_query, url: results_path, method: :get do |f| %>
        <div class="form-inputs">
          <%= f.input :sport, autofocus: true, collection: @sports_selection, prompt: "Pick a sport  ▾", label: false %>
          <%= f.input :date, as: :date_picker, placeholder: "Pick a date", label: false %>
          <%= f.button :submit, "Search", class: "search-button form-inline" %>
        </div>
      <% end %>
     </div>
    </div>
    <div class="banner-photo" style="background-image: url(<%= image_url 'woman-running-park-sunset1.jpg'%>);">
    </div>
  </div>
  <div class="banner-photo" style="background-image: url(<%= image_url 'woman-running-park-sunset1.jpg'%>);">
  </div>
</div>

<div class="hidden-sm hidden-md hidden-lg">
  <div class="banner-photo-xs" style="background-image: url(<%= image_url 'woman-running-park-sunset1.jpg'%>);">
  </div>
  <div class="banner-title-xs">
    <h1 class="text-center">
     Soplex
   </h1>
   <h4 class="text-center" id="xs-tagline">
     Train Whenever, Wherever
   </h4>
 </div>
 <div class=" xs-banner search-field-banner">
  <%= simple_form_for :search_query, url: results_path, method: :get do |f| %>
  <div class="text-center form-inputs">
    <%= f.input :sport, autofocus: true, collection: @sports, prompt: "Pick a sport ▼", label: false, class: "black-button" %>
    <%= f.input :date, as: :date_picker, placeholder: "Pick a date", label: false %>
    <%= f.button :submit, "Search", class: " searchs-button navbar-wagon-button form-inline" %>
  </div>
  <% end %>
</div>


</div>

